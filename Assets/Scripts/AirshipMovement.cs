using UnityEngine;

public class AirshipMovement : MonoBehaviour
{
	//This will be similar to PlayerMovement,
	//with an additional system to change camera and controls.
	
	//However, both this AND playermovement are precluded by having animations for which to move...
	//Though this can be started with maybe simpler mechanics...
	//Need to make a better player sprite with more animations for testing, though.
	
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
